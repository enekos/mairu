## mairu ingest

Parse an MD file or free text via LLM into context nodes, review, then persist

```
mairu ingest [file] [flags]
```

### Options

```
      --base-uri string   Base URI namespace for generated nodes (default "contextfs://ingested")
  -h, --help              help for ingest
      --no-router         Skip LLM dedup router when persisting nodes
  -P, --project string    Project namespace (default "default")
      --text string       Free text to ingest
  -y, --yes               Skip interactive review and persist all proposed nodes
```

### Options inherited from parent commands

```
      --debug           Enable debug logging
  -o, --output string   Output format: table, json, plain (default "table")
      --quiet           Only output results, no status messages
      --verbose         Show extra details (timing, weights, query plan)
```

### SEE ALSO

* [mairu](mairu.md)	 - Mairu - The coding agent that knows your codebase

###### Auto generated by spf13/cobra on 8-Apr-2026
