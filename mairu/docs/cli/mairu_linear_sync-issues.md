## mairu linear sync-issues

Sync Linear issues into context nodes

```
mairu linear sync-issues <team_key> [flags]
```

### Options

```
  -h, --help           help for sync-issues
  -l, --limit int      Limit the number of issues to fetch (default 50)
  -s, --state string   Filter by state name (e.g. 'In Progress', 'Done')
```

### Options inherited from parent commands

```
      --debug            Enable debug logging
  -o, --output string    Output format: table, json, plain (default "table")
  -P, --project string   Project name
      --quiet            Only output results, no status messages
      --verbose          Show extra details (timing, weights, query plan)
```

### SEE ALSO

* [mairu linear](mairu_linear.md)	 - Linear integration commands

###### Auto generated by spf13/cobra on 8-Apr-2026
