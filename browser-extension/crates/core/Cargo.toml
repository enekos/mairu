[package]
name = "browser-extension-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
scraper = "0.26.0"

[dev-dependencies]
insta = { version = "1.47.2", features = ["glob", "json"] }
