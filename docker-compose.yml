services:
  meilisearch:
    image: getmeili/meilisearch:v1.12
    container_name: mairu-meili
    ports:
      - "7700:7700"
    environment:
      MEILI_MASTER_KEY: "contextfs-dev-key"
      MEILI_ENV: "development"
    volumes:
      - meili_data:/meili_data

volumes:
  meili_data:
    driver: local
